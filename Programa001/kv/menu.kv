<TelaMenu>:

    BoxLayout:
        orientation: 'vertical'
        Label:
            text: 'Bem-vindo à menu!'
        Button:
            text: 'Voltar'
            on_press: root.manager.current = 'login'
            # quando clicar, altera a current para login e muda a tela